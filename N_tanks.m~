% The method to estimate N from n observations
% collected from uniform distribution 1, 2, ..., N.
% Idea from the German Tank p

function estimate_for_N = N_tanks(N, n)
  sample = randi(N, [n, 1]); 
  m = max(sample); 
  k = length(sample); 
  estimate_for_N = m * (1 - (1/k)) - 1; 
end 