% Plots the sum of an infinite geometric series with an initial value a and
% a ratio of consecutive terms, q.

function infinite_geometric_series_sum(a, q)
  v = 0.90:0.002:0.998; 
  x = ones(50, 1); 
  o = ones(50, 1); 
  
  for i = 1:50 
    x(i) = (1 / (1 - v(i))); 
  end 
  
  x = x ./ (o - v); 
  
  plot (v, x, 'linewidth', 2); 
  grid on; 
  % sum = a / (1 - q);
end 